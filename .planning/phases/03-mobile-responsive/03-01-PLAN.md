# Plan 03-01: Core Layout & Navigation ✓ COMPLETED

## Goal
Implementar navegación responsive con menú hamburguesa en móvil, Footer adaptativo, y MainLayout optimizado para todos los viewports.

**Status:** COMPLETE
**Completed:** 2026-01-31
**Duration:** ~5 min

## Analysis Summary

### Files to Modify

| File | Issue | Priority |
|------|-------|----------|
| `apps/web/src/components/Header.jsx` | Falta menú hamburguesa para móvil, nav links visibles en todas las resoluciones | CRITICAL |
| `apps/web/src/components/Footer.jsx` | Flex sin wrap, márgenes fijos (ml-10), layout horizontal no colapsa | CRITICAL |
| `apps/web/src/layouts/MainLayout.jsx` | Verificar padding/margin responsivos | HIGH |
| `apps/web/src/components/SearchBar.jsx` | w-80 fijo, no se adapta a móvil | HIGH |

### Technical Approach

**Mobile-First Principles:**
1. Estilos base para móvil (320px+)
2. Breakpoints progresivos: sm:640px, md:768px, lg:1024px
3. Touch targets mínimo 44x44px
4. Font-size 16px mínimo en inputs (evita zoom iOS)

## Tasks

### 1. Header con Hamburger Menu
**File:** `apps/web/src/components/Header.jsx`

```jsx
// Estructura objetivo:
// Mobile (<md): Logo | Hamburger button
//   → Drawer/menu desplegable con: Search, Inicio, Catálogo, Contacto
// Desktop (>=md): Logo | Search | Inicio | Catálogo | Contacto (actual)

// Agregar:
// - useState para controlar apertura del menú
// - Botón hamburguesa visible solo en móvil (md:hidden)
// - Nav links ocultos en móvil (hidden md:flex)
// - Drawer/overlay para menú móvil
// - Animación suave de entrada/salida
```

**Subtasks:**
- [ ] Agregar estado `isMenuOpen` para controlar drawer
- [ ] Crear botón hamburguesa (3 líneas, 44x44px touch target)
- [ ] Ocultar nav links en móvil: `hidden md:flex`
- [ ] Crear drawer/overlay para menú móvil
- [ ] Agregar SearchBar dentro del drawer en móvil
- [ ] Implementar cierre al hacer click fuera o en link
- [ ] Agregar transiciones CSS para apertura/cierre

### 2. Footer Responsive
**File:** `apps/web/src/components/Footer.jsx`

```jsx
// Estructura objetivo:
// Mobile: Stack vertical (Logo arriba, info abajo)
// Desktop: Horizontal como está ahora

// Cambios:
// - flex-col en móvil, flex-row en desktop
// - Eliminar ml-10 fijos, usar gap responsivo
// - Centrar contenido en móvil
// - Ocultar divisor en móvil
```

**Subtasks:**
- [ ] Cambiar flex a `flex-col md:flex-row`
- [ ] Reemplazar `ml-10` con `gap-4 md:gap-8`
- [ ] Divisor: `hidden md:block`
- [ ] Centrar texto en móvil: `text-center md:text-left`
- [ ] Ajustar tamaño logo para móvil
- [ ] Links con touch target adecuado (py-1)

### 3. SearchBar Responsive
**File:** `apps/web/src/components/SearchBar.jsx`

**Subtasks:**
- [ ] Reemplazar `w-80` con `w-full md:w-80`
- [ ] Asegurar input height >= 44px para touch
- [ ] Font-size 16px para evitar zoom en iOS

### 4. MainLayout Verification
**File:** `apps/web/src/layouts/MainLayout.jsx`

**Subtasks:**
- [ ] Verificar padding responsivo del container
- [ ] Asegurar que no hay overflow horizontal
- [ ] Verificar que SelectionBar no interfiere con layout móvil

## Definition of Done

- [ ] Header muestra hamburguesa en móvil, links en desktop
- [ ] Menú hamburguesa abre drawer con navegación
- [ ] Footer se apila verticalmente en móvil
- [ ] No hay scroll horizontal en viewport 320px
- [ ] Touch targets >= 44x44px en todos los elementos interactivos
- [ ] Transiciones suaves en apertura/cierre de menú

## Estimated Duration
~25 min

## Testing Checklist

1. **Viewports a probar:** 320px, 375px, 768px, 1024px, 1920px
2. **Navegadores:** Chrome DevTools mobile simulation
3. **Interacciones:**
   - Abrir/cerrar menú hamburguesa
   - Navegar con menú abierto
   - Buscar desde menú móvil
   - Verificar footer en todos los viewports
